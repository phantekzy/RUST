// Control flow
// If Expressions
// using if in a let statement
fn main() {
    let condition = false;
    let number = if condition { 5 } else { "six" };
    println!("the value of number is {}", number);
}
