fn main() {
    // The Array type

    // Invalid Array elements
    let a = [0, 1, 2, 3, 4, 5];
    let index = 10;
    let element = a[index];

    println!("The value of element is : {} ", element);
}
